{
  "name": "LiteLLM",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "postCreateCommand": "echo 'export GITHUB_API_KEY=$GITHUB_TOKEN' >> ~/.bashrc && chmod +x setup.sh",
  "postStartCommand": "bash -c 'source ~/.bashrc && ./setup.sh'"
}
